<%= form_for(@centroscomercial) do |f| %>
  <% if @centroscomercial.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@centroscomercial.errors.count, "error") %> prohibited this centroscomercial from being saved:</h2>

      <ul>
      <% @centroscomercial.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br>
    <%= f.text_field :nome %>
  </div>
  <div class="field">
    <%= f.label :cidade_id %><br>
    <%= f.text_field :cidade_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
